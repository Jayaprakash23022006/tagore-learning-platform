# Netlify Configuration for Tagore Learning Platform
# React SPA — all routes redirect to index.html

[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"
